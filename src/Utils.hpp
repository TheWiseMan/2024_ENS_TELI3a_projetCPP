#include <string>
#include <list>
#include <map>
#include <iostream>

using namespace std;

namespace Utils
{
    void displayMap(map<string, string> map);
    void displayMapList(list<map<string, string>> list);
}